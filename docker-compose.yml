version: '3'
services:
  hugo:
    image: 'docker.io/peaceiris/hugo:v${HUGO_VERSION}'
    command:
      - server
      - --bind=0.0.0.0
      - --buildDrafts
    ports:
      - 127.0.0.1:1313:1313
    user: 1000:100
    volumes:
      - .:/src
